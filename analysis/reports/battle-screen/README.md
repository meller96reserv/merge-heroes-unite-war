# First shared RN/Skia battle composition

TASK-0059/0060/0061 reconstruct the measured HUD, world, board and five-tab composition from Figma2:426. Inputs are the preserved local archive/semantic map, acquired fonts and the saved design context. The extra currency frame is the text-free authored component2:487; its provenance/hash is in app/assets/component-exports.json. No complete screenshot is imported as UI. All labels, heroes, platforms, tab selection and input owners remain separate live elements.

Bounded acceptance: source anchors and canonical dimensions match the measured screen specification (currency x61/225,stage y189,board5columns at x15+83c with549/651 rows,nav7+84c/y847); header typography uses the acquired Passion One fonts. Exact10-slot and functional15-slot fixtures remain distinct. Ten viewport/fixture cases have one canvas,zero client errors,valid purchase targets and identical frozen state after modal return. Every implemented control has visible Skia press feedback and an accessible input owner. Fast Refresh changes rendered Skia text while preserving an open modal,without navigation. Expo production export starts the same scene and excludes debug fixture data. Native runtime acceptance is NOT_RUN.

Pixel fidelity is **not final PASS**: unmasked canonical differing-pixel fraction at tolerance16 is7.64%,above the initial1.5% target. The full overlay/diff is retained without enlarged masks. Remaining differences include glyph rasterization,font baselines,source-image resampling and small character/frame geometry; TASK-0069 owns full static golden acceptance across all15screens. This report accepts the bounded reconstructed components,not a final exact-fidelity/release gate. Compact layout compresses world art above the board; later mobile visual acceptance remains required.

Sources remain unchanged. Density-aware128/256 derivatives reduce minification artifacts while retaining full source canvases and semantic IDs; high-density/large views use the larger source-backed runtime assets. The source/hash registry validator covers all63derivatives plus the authored frame. These are local preview assets; this task does not grant distribution rights.

The actual application opens at `npm run dev:web` / http://localhost:8082. Static fixture URLs are `?fixture=figma-battle-10` and `?fixture=adapted-battle-15` in development only. Current component tasks use view data; purchase/merge/combat authority is connected by the subsequent core DAG. Existing foundation/orb tests and evidence describe their historical checkpoints; this harness replaces them for the current composition.

Planning is complete. Production implementation is now authorized.

Do NOT create another high-level planning pass.

The planning package already present in this repository is the authoritative implementation specification. Your job now is to execute it autonomously from beginning to end, following the atomic task DAG, acceptance criteria, evidence rules, architecture decisions, Figma manifests, schemas, gameplay specifications, visual specifications, animation/VFX/audio specifications, QA plans, and release gates already created.

---

# 1. Read the existing implementation authority first

Before making production implementation changes, read:

- `CODEX_EXECUTION_RULES.md`
- `PLANNING_COMPLETE.md`
- `docs/00_INDEX.md`
- `docs/02_SOURCE_OF_TRUTH.md`
- `docs/03_EVIDENCE_LEDGER.md`
- `docs/04_UNKNOWNS_REGISTER.md`
- `docs/conflicts.md`
- `plans/00_EXECUTION_ORDER.md`
- `plans/01_DEPENDENCY_GRAPH.md`
- `plans/02_CRITICAL_PATH.md`
- `tasks/TASK_INDEX.md`
- `tasks/task_manifest.json`

Then inspect every additional document referenced by the currently executable task.

Do not implement from phase names or task titles alone.

The atomic task dependency graph is authoritative.

Numeric phase order is not execution order.

Begin with `TASK-0001 — Planning handoff validation`.

After completing each task, resolve the next actually executable task from `tasks/task_manifest.json`.

---

# 2. AGENTS.md

`AGENTS.md` may currently be empty.

As part of the initial implementation handoff, populate it with concise durable repository-wide instructions derived from:

- `CODEX_EXECUTION_RULES.md`
- the architecture documents
- source-of-truth/evidence rules
- this implementation instruction

Do not duplicate the complete planning package into `AGENTS.md`.

Use it as a short permanent guardrail for future coding sessions.

At minimum preserve these rules:

- use the task DAG;
- read relevant specs before implementation;
- keep `game-core` pure and deterministic;
- React Native owns native/system host concerns;
- Cocos owns game presentation/rendering/input;
- presentation must not own authoritative rewards, damage, currency, merge results or progression;
- preserve OBSERVED / MEASURED / DERIVED / PROPOSED / UNKNOWN distinctions;
- never mark NOT_RUN or BLOCKED as PASS;
- preserve the original `.fig`;
- use semantic Figma asset IDs;
- do not use full-screen screenshots as interactive UI;
- do not silently substitute WebView/Flutter for native RN+Cocos integration;
- run acceptance criteria before marking tasks complete.

---

# 3. Development environment ownership

You are responsible for bootstrapping and maintaining the development environment required to execute this project.

Do not stop merely because a required development tool, SDK, package manager dependency, CLI, runtime, simulator support package, build utility, or project dependency is missing.

Before beginning implementation, audit the actual machine/environment against the toolchain required by the plans and atomic tasks.

For every missing prerequisite that can be installed safely and autonomously:

1. Determine the exact required or compatible version from the repository specifications.
2. Prefer pinned/reproducible versions over "latest".
3. Install it using the safest supported installation method available on the current OS.
4. Verify the installation by running its version/status command or an equivalent real smoke test.
5. Record the installed version and relevant paths/configuration in the project environment documentation.
6. Continue execution without asking the user merely to perform routine installation steps.

This explicitly includes, when required:

- Cocos Creator
- Node.js / npm tooling
- React Native tooling
- Android SDK components
- Android build-tools
- Android platform packages
- Android NDK
- CMake / Ninja
- JDK
- Gradle dependencies
- adb / emulator tooling
- CocoaPods
- Ruby tooling required by CocoaPods
- Xcode command-line tooling
- iOS simulator runtimes where installable
- project package dependencies
- schema validators
- image/asset processing utilities
- test runners
- linting/formatting/build utilities
- other development-only dependencies required by an executable task

A missing tool is NOT a blocker by itself.

Attempt safe autonomous installation/configuration first.

---

# 4. Cocos Creator

The planning direction is Cocos Creator 3.8 LTS, but use the exact compatible patch/version resolved by the repository toolchain-lock task.

Do not arbitrarily install another major/minor version merely because it is easier.

If Cocos Creator is absent:

1. resolve the exact/compatible version according to `TASK-0037` and the relevant architecture/toolchain documents;
2. install it using an official/supported mechanism;
3. locate and record the executable/editor path;
4. verify the version;
5. verify that the required project/build/export workflow actually works;
6. configure reproducible repository scripts or documented commands where practical;
7. execute the standalone Cocos export/smoke probe required by `TASK-0038`.

Do not treat Cocos absence as an immediate user blocker.

---

# 5. Installation safety

Prefer, in order:

1. repository-local dependencies;
2. version managers;
3. standard OS/package-manager installations;
4. official vendor installers/downloads.

Do not:

- uninstall unrelated user software;
- unnecessarily upgrade unrelated global software;
- replace an already compatible toolchain merely because a newer version exists;
- delete existing SDKs/toolchains;
- disable OS security mechanisms;
- bypass signing/security controls;
- invent credentials or licenses;
- destructively alter unrelated shell/system configuration.

PATH/environment changes are allowed when necessary, but keep them minimal and document them.

Use elevated privileges only when legitimately required and available.

Request user action only when automation is genuinely impossible, for example:

- an unavoidable OS permission/security dialog;
- administrator authentication unavailable to you;
- Apple Developer login;
- provisioning/signing requiring the user's account;
- unavoidable Cocos/vendor authentication;
- physical device connection/unlock/trust;
- an unavoidable GUI-only installer step;
- unavailable external resource/network access;
- an irreversible/security-sensitive choice.

Before reporting such a blocker, complete every independent non-blocked step first.

When blocked, report exactly:

- what is already installed/configured;
- what remains;
- why it cannot be automated;
- the exact user action required;
- the exact task to resume afterward.

Do not merely say "install X manually".

---

# 6. Preserve the planned architecture

Do not casually redesign the architecture.

The intended direction is:

- React Native = mobile/native application host;
- Cocos Creator = game renderer/presentation/game-space interaction;
- pure TypeScript `game-core` = deterministic authoritative gameplay/domain logic where specified;
- typed/versioned bridge = RN ↔ Cocos communication;
- schemas/data/config = tunable data-driven behavior.

`game-core` must remain independent of:

- Cocos APIs;
- React Native APIs;
- renderer-specific state;
- arbitrary UI components.

Domain/game-core owns authoritative behavior such as, where specified:

- money/currency mutations;
- merge result;
- damage;
- progression;
- reward receipts;
- stage state;
- deterministic economy;
- persistent game state.

Cocos projects state and routes input/presentation.

React Native owns native/system/application integration.

Do not generate rewards or authoritative damage from animation/tween completion.

Do not send unnecessary per-frame traffic through the native bridge.

---

# 7. Native RN + Cocos feasibility is an early hard gate

Do not begin large-scale production gameplay implementation before the required native integration feasibility gate is successfully executed.

Complete its prerequisites first.

The expected early chain includes the planned tasks such as:

- `TASK-0037 — Native toolchain lock`
- `TASK-0038 — Standalone Cocos export probe`
- React Native standalone host prerequisites
- native integration tasks
- bridge/lifecycle checks
- the complete native acceptance ledger

Execute the actual `PHASE_03_NATIVE_INTEGRATION_SPIKE` according to its documents.

NAT-01…NAT-15 require real evidence.

A result of:

- `NOT RUN`
- `BLOCKED`
- `UNKNOWN`

is never a PASS.

Test actual behavior wherever executable, including:

- Android build/install/launch;
- RN host screen;
- Cocos rendering;
- touch;
- Cocos → RN bridge;
- RN → Cocos bridge;
- Android Back;
- repeated enter/leave;
- background/resume;
- audio focus;
- engine ownership;
- process-death/save handshake;
- malformed/duplicate/stale bridge messages;
- iOS equivalent where environment permits;
- final ADR decision.

Do not infer native feasibility from source code inspection or browser success.

Do not silently replace the planned native integration with:

- WebView;
- Flutter;
- another engine;
- fake/simplified host architecture.

If the preferred strategy genuinely fails, follow the existing ADR/escalation process and collect evidence before changing strategy.

---

# 8. Continuous browser preview

In addition to native validation, maintain a continuously usable browser preview of the Cocos game throughout development.

The browser preview is a first-class development workflow.

As soon as the first Cocos scene can render, establish a reproducible browser development command.

Prefer something simple such as:

- `npm run game:web:dev`
- `npm run game:web`
- or an equivalent repository-standard command.

Document the exact command in the README/development documentation.

The goal is that the user can easily launch the current state of the game in a desktop browser at any meaningful gameplay/visual milestone.

Do not make browser preview a separate simplified implementation.

The browser preview must use the same production:

- `game-core`;
- gameplay rules;
- balance/config data;
- Cocos scenes;
- prefabs;
- Figma-derived assets;
- animations;
- VFX;
- gameplay state;
- event contracts

as the native Cocos game wherever platform-independent.

Use browser preview for rapid daily iteration of:

- battle;
- merge board;
- UI;
- animations;
- VFX;
- hero/enemy presentation;
- stage progression;
- layout;
- Figma visual reconstruction.

Native-specific functionality must still be tested natively.

---

# 9. Browser development bridge harness

Because React Native/native APIs are unavailable in ordinary browser preview, implement a clearly development-only web bridge adapter/harness that implements the same typed logical bridge interface where practical.

For example:

production:
`NativeBridgeAdapter`

browser development:
`WebDevBridgeAdapter`

The gameplay/Cocos code should communicate against an abstraction, not hardcode the browser mock.

The development bridge may simulate, when needed:

- session/user initialization;
- app lifecycle events;
- safe-area/device information;
- save/load commands;
- native acknowledgements;
- rewarded-ad responses;
- purchase responses;
- haptic acknowledgements;
- process-resume style events.

The browser harness must never become production authority.

Keep all mocks/dev-only behavior excluded or clearly disabled in production builds.

---

# 10. Browser development controls

Where useful, create a lightweight development/debug panel for browser preview.

It may expose controls such as:

- set stage;
- spawn enemy;
- spawn boss;
- add/remove gold;
- set hero tier;
- generate hero;
- fill/clear board;
- trigger merge;
- toggle auto-merge;
- trigger animation event;
- trigger VFX event;
- trigger audio event;
- simulate offline duration;
- save/load/reset state;
- choose representative phone aspect ratio;
- toggle reduced motion;
- display FPS/entity/pool diagnostics.

These controls are for development and QA only.

They must not leak into release builds.

Do not spend excessive production time building a sophisticated editor. Keep it lightweight and useful for iteration/debugging.

---

# 11. Figma assets are first-class production input

Use the existing Figma analysis, semantic asset map, manifests, screen maps and visual specifications.

Preserve the original `.fig`.

Do not destructively modify it.

Do not use whole-screen screenshots as interactive production UI.

Do not automatically cut flattened characters into invented body pieces without explicit source support.

Use semantic asset IDs rather than arbitrary filenames where specified.

Preserve:

- source provenance;
- calibrated pivots;
- trim/insets;
- dimensions;
- screen mapping;
- state mapping.

Use actual mapped assets whenever they exist.

If a final asset is genuinely missing, record it according to the existing asset-gap process instead of silently presenting an invented placeholder as final.

---

# 12. Animation, VFX, audio and haptics are implementation requirements

Do not postpone all motion until the end.

Implement the planned animation/event contracts throughout feature development.

Use the existing semantic IDs and specifications.

Respect planned fields and concepts such as:

- trigger;
- duration;
- easing;
- release/critical markers;
- interrupt policy;
- VFX event;
- audio event;
- haptic event;
- pooling;
- concurrency limit;
- quality fallback.

The important interactions must feel polished, including where specified:

- hero spawn;
- purchase;
- pickup/drag/drop;
- valid/invalid slot feedback;
- merge;
- attack anticipation/release;
- projectile;
- impact;
- damage numbers;
- enemy hit/death;
- boss intro/death;
- reward;
- stage completion;
- slot unlock;
- UI presses;
- daily/wheel/chest interactions.

Presentation may visually react to authoritative game events but must never become the authoritative gameplay state.

---

# 13. Audio

Implement the semantic audio architecture even if final production audio assets do not yet exist.

Where final sound assets are missing:

- keep semantic event IDs stable;
- use silence or clearly labeled temporary development assets where useful;
- track missing final production audio;
- do not pretend placeholders are final;
- make eventual asset replacement straightforward.

Use only original/licensed distributable audio for release.

Do not extract reference-game audio or private assets.

---

# 14. Unknown/reference mechanics

Preserve all existing evidence classifications.

Do not invent exact reference-game behavior.

For each UNKNOWN with an assigned verification task:

1. inspect existing evidence;
2. perform the planned bounded verification/research;
3. record actual observations;
4. update evidence/unknown records appropriately.

If exact reference behavior cannot reasonably be established and the project can continue:

- leave the reference fact UNKNOWN;
- define a deterministic clearly marked `PROPOSED` product rule;
- keep its tunable parameters data-driven;
- document the decision;
- create/run acceptance tests;
- continue implementation.

Do not indefinitely block the whole project trying to discover an inaccessible exact formula.

But never relabel a proposed rule as OBSERVED.

---

# 15. Atomic task execution discipline

For every task:

### Before coding

1. verify all dependencies are actually complete;
2. read the full atomic task;
3. read every referenced specification;
4. inspect relevant existing code;
5. inspect relevant schema/data;
6. inspect relevant Figma asset mapping if visual;
7. understand acceptance criteria.

### Implement

Implement the smallest coherent bounded change satisfying the task.

Reuse existing systems.

Do not create parallel duplicate architecture.

Do not mass-create empty/stub files merely to make progress appear complete.

### Verify

Run every meaningful applicable:

- unit test;
- integration test;
- schema validation;
- build;
- smoke test;
- runtime test;
- visual check;
- manual acceptance case

required by the task.

Record actual expected/actual evidence.

Only mark the task complete when its own oracle/acceptance criteria are met.

Code existing in the repository is not sufficient evidence by itself.

---

# 16. Persistence correctness

Preserve the planned durable transaction/save architecture.

Correctly implement and test where specified:

- idempotency;
- receipts;
- generations/watermarks;
- stale message rejection;
- A/B save recovery;
- corruption handling;
- migrations;
- app restart;
- process death;
- offline progression;
- duplicate reward prevention.

Do not trade correctness for convenience without measurement and an ADR.

If save throughput becomes an actual performance bottleneck, profile it first and use the documented redesign process.

---

# 17. Performance

Profile real representative gameplay.

Observe planned budgets for:

- frame time;
- allocations;
- texture memory;
- node counts;
- bridge traffic;
- particles;
- projectiles;
- floating text;
- tweens;
- event listeners;
- pooled objects;
- audio voices;
- repeated RN↔Cocos enter/exit;
- background/resume;
- save frequency.

Use pooling and quality fallback where specified.

Check for lifecycle/resource leaks.

Do not optimize purely from speculation.

---

# 18. Mobile visual behavior

Implement the Figma reference composition while supporting the planned mobile aspect-ratio strategy.

Test representative mobile dimensions.

Respect:

- safe areas;
- reference resolution;
- anchors;
- touch-target sizes;
- camera/layout rules;
- cropping/fill rules.

Do not arbitrarily stretch artwork.

Browser preview should include convenient representative aspect-ratio testing where practical.

---

# 19. Keep the project playable

Do not generate hundreds of disconnected source files before attempting to run anything.

Maintain executable/playable milestones.

Examples include:

- toolchain ready;
- Cocos standalone scene;
- RN standalone shell;
- native Cocos host;
- bidirectional bridge;
- browser preview;
- Figma battle screen;
- first hero;
- first enemy;
- attack;
- hit/death;
- reward;
- merge board;
- merge;
- stage/boss loop;
- persistence;
- vertical slice;
- MVP.

At meaningful milestone boundaries:

- build;
- run;
- verify;
- fix regressions.

---

# 20. Git and user work safety

Preserve user changes and repository history.

Do not restore deleted historical application code as an assumed foundation.

Do not destructively reset unrelated changes.

Do not modify the original source Figma file.

Prefer coherent bounded changes/checkpoints.

Do not commit:

- credentials;
- secrets;
- caches;
- unnecessary huge generated build output;
- temporary diagnostic exports

unless repository policy explicitly requires them.

---

# 21. Do not ask for routine engineering decisions

Make ordinary reversible engineering decisions autonomously.

Do not stop to ask whether you should:

- install a routine dependency;
- fix a compile error;
- run a required test;
- retry an installation;
- add a necessary project-local script;
- choose an internal implementation detail compatible with the architecture.

Use professional judgment and continue.

---

# 22. When you may stop

Continue autonomously for as long as executable work remains.

You may stop only for a genuine external blocker such as:

- unavailable human credentials;
- unavoidable admin authentication;
- unavoidable OS GUI/security approval;
- Apple Developer/signing access;
- unavoidable vendor login/license;
- required physical-device interaction unavailable to you;
- inaccessible external resource;
- irreversible business/product decision not covered by the existing plans;
- a proven architecture failure requiring user-level product/technology choice.

A normal coding/build/test failure is NOT a reason to stop.

Debug it.

A missing package is NOT a reason to stop.

Install it when safe.

An UNKNOWN reference mechanic is not automatically a reason to stop independent work.

Follow its evidence/proposed-behavior rules.

Before stopping, finish any remaining independent executable work that does not violate a hard gate.

---

# 23. Blocker report

If genuine user intervention becomes unavoidable, report only after preserving a coherent working state.

Report:

- last completed TASK ID;
- current phase/milestone;
- exact blocker;
- evidence/log/result proving it;
- what you already attempted;
- what remains;
- whether independent tasks remain executable;
- the exact user action required;
- the exact TASK ID/command to resume afterward.

Do not give a vague blocker description.

---

# 24. Progress visibility

Maintain useful implementation progress records using the existing task/status system.

The user should be able to determine:

- current task;
- completed tasks;
- current milestone;
- blocked tasks;
- next executable tasks;
- native spike status;
- browser preview command/status.

Do not create a redundant giant planning system if the current task manifest already supports this.

Keep status updates concise and machine-maintainable.

---

# 25. Final implementation objective

Continue through the existing DAG toward the planned vertical slice and MVP, not merely until the first scene launches.

The intended vertical slice already defined by the planning package includes the end-to-end flow:

native portrait launch
→ Figma-based battle HUD/board
→ hero purchase
→ merge/discovery
→ deployment
→ automatic combat
→ projectile/hit/death
→ reward
→ next stage/boss
→ durable save
→ restart/resume

with the planned animation/VFX/audio/settings support.

After the native feasibility gate succeeds, continue autonomously through all executable implementation phases/tasks toward the defined MVP and QA gates.

Do not redefine MVP from this prompt; use the repository's existing MVP specification.

---

# 26. Start now

Start by executing `TASK-0001`.

Do not respond with another implementation plan.

Inspect the repository, validate the handoff, bootstrap the required environment, and begin executing the existing task DAG.

As soon as the dependencies allow it:

- resolve the native toolchain lock;
- install/configure missing Cocos/toolchain dependencies;
- establish the standalone Cocos smoke build;
- establish the continuous browser preview workflow;
- execute the RN+Cocos native feasibility spike with real evidence.

Then continue autonomously according to the dependency graph.

# Native audio checkpoint — TASK-0044

**PARTIAL.** The coherent simulator implementation and its verified cases are preserved here. The full original task is not marked complete: physical headphones and a real iOS audio interruption were not tested. The user has directed a production pivot to React Native + Skia after this checkpoint; this Cocos spike is historical evidence, not production architecture approval.

On the private Android API36 emulator, the [eight-case run](evidence/audio-android-resume02.json) passed loop plus one-shot playback, mute, suppressed SFX while muted, muted background/resume, three unmuted resumes, unchanged bridge rejection counts, and mute across Cocos scene recreation. The same process11542 remained alive. After unmute, loop starts advanced2→3→4→5 exactly once per resumed unmuted interval.

A [separate test application](focus-interrupter/FocusInterrupter.java), UID10217, then requested real transient system focus while Cocos remained visible under UID10214. At00:34:58.238 the request succeeded; the host received LOSS_TRANSIENT and both players stopped at00:34:58.282. Abandon at00:35:01.248 caused GAIN and one loop restart at00:35:01.273, starts2→3. [Native log](logs/audio-android-external-focus02.log). The companion targetsAPI34 so it can make a delayed background focus request; the actual game probe remains targetAPI36. No synthetic focus callback was substituted for this case.

On iPhone17Pro/iOS26.2 Simulator, process33979 passed SFX, mute/suppression, one muted and three unmuted Home/resume cycles, then two retained-scene entries. Starts2 remained2 across muted resume; unmute produced3; subsequent resumes produced4,5,6. Reopen produced7; mute persisted on the next entry with starts7; unmute produced8. [Native log](logs/audio-ios-runtime-final.log), [flow results](logs/audio-ios-resume02-driver.log), [reopen results](logs/audio-ios-reopen03-driver.log), [muted screenshot](evidence/audio-ios-reopened-muted.png), [unmuted screenshot](evidence/audio-ios-reopened-unmuted.png). The last buffered Cocos stdout records are incomplete; final reopen counts are visually verified screenshot observations, not fabricated log events. One extra automation-induced inactive/active pair preceded the controlled flow. The [Hermes log](evidence/audio-ios-resume02-hermes.json) reports the original9/9 rejection suite PASS and no unexpected transport failure.

The [same Cocos browser export](evidence/audio-web-gesture02.json) passed two loop starts, two SFX starts and one muted SFX suppression after an actual user gesture. The first attempt clicked before scene readiness and is retained as [FAIL](evidence/audio-web-controls.json). Browser success proves neither native focus nor acoustic output.

The implementation uses [original diagnostic PCM](probe-audio.md), one loop source and one stoppable SFX source. Native focus plus visible state plus unmuted settings permits playback; duplicate state notifications do not multiply loops. Twenty-one tests in three suites pass, including late loading, closed gates, duplicate gains and paused transport sequence preservation. Build-driver logs and exact source/evidence SHA256 values are in [audio-results.json](audio-results.json).

Two native defects were reproduced and repaired. CocosActivity's default focus owner competed with the host owner; the adapter removes its four registration calls only in a hash-checked build copy and preserves the installed editor. An onShow ping preceded Android onResume and was dropped; the host now sends fresh audio state after its transport becomes active, while inactive states are cached. Failed attempts remain in the logs. No production gameplay, final sound assets, physical routing, acoustic measurements or durable process-death mute is claimed.

Verification: `python3 spikes/rn-cocos/verify_audio.py`; `python3 spikes/rn-cocos/generate_probe_audio.py --check`. Re-running native probes is unnecessary for the directed architecture pivot. Relevant OS behavior: [Android audio focus](https://developer.android.com/media/optimize/audio-focus), [Apple audio interruptions](https://developer.apple.com/documentation/avfaudio/handling-audio-interruptions).

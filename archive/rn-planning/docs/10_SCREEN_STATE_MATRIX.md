# Матрица screen states

[PROPOSED] Каждая строка дополняет детальный screen spec; domain event determines state, not animation callbacks.

| Screen | State | Visible nodes | Enabled input | Transitions | Timers | Audio | Persistence | Race policy | Interruption |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SCREEN-001 | loading | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-001 | load_failed | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-001 | retry | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-002 | ready | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-002 | pressed | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-002 | restoring | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-002 | error | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | first_launch | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | tutorial_buy_prompt | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | normal_idle | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | hero_purchase_available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | insufficient_gold | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | board_full | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | merge_available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | auto_merge_running | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | stage_in_progress | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | boss_warning | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | boss_combat | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | stage_clear | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | stage_failed | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | feature_unlock | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | reward_flying | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | modal_open | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | app_paused | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-003 | returning_from_background | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | selected | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | empty_slot | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | locked_slot | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | equipped | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | affordable | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | insufficient | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | max_level | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-004 | opening | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-005 | collection | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-005 | empty | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-005 | locked | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-005 | opening | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-005 | reward_pending | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-005 | insufficient | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-006 | locked | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-006 | available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-006 | loading | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-006 | active | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-006 | failed | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-006 | completed | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-007 | list | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-007 | scrolling | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-007 | available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-007 | insufficient | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-007 | maxed | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-007 | selected | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-008 | available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-008 | committing | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-008 | claimed | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-008 | error | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-009 | available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-009 | committing | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-009 | claimed | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-009 | error | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-010 | normal | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-010 | muted | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-010 | permission_denied | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-010 | save_error | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-011 | open | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-011 | editing | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-011 | closing | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-011 | permission_denied | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-012 | unavailable | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-012 | retrying | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-012 | recovered | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-013 | available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-013 | spinning | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-013 | result | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-013 | error | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-014 | free_available | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-014 | spinning | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-014 | result | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-015 | cooldown | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-015 | expired | base HUD + state-specific affordance | eligible commands + navigation | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |
| SCREEN-015 | app_paused | base HUD + owning overlay | owning action disabled; close per contract | command result / timer / lifecycle | simulation paused only on background; UI time separate | semantic audio after commit | critical transaction checkpoint; UI transient | dedupe token; stale revision rejected | cancel effects; reconcile snapshot |

[Screen catalog](09_SCREEN_CATALOG.md) · [Game state](17_GAME_STATE_MODEL.md) · [Tasks](../tasks/TASK_INDEX.md).

# Six-hour runnable candidate mode

User resumed at 2026-09-09 12:57 UTC from b83a2ab. This targeted execution override
preserves all required TZ/Figma functionality and the RN/Expo/Skia architecture.
It supersedes older per-task ceremony and formal release certification gates.

Group coherent existing task IDs into runtime milestones: early native launch;
entry/equipment/hero upgrades; daily/relic; three dragons using existing battle;
required native services/ads; final polish and Android/iOS Simulator builds.
Wire each subsystem to its required browser screen before another disconnected
subsystem. Root `npm run dev:web` stays the shared Fast Refresh preview.

The manifest retains deferred IDs/specifications. Standalone CI/replay/research,
content export/duplicate validation, exhaustive migration/crash/visual/audio/
lifecycle/performance QA and formal rights/distribution ceremony do not block
this candidate. Actual required content lives in implementation-owned configs;
actual defects, economic invariants, ad completion, current saves and native
build/runtime failures remain mandatory. No required feature is deferred.

Use targeted compilation, changed business/state tests and one browser smoke.
Reuse unchanged green results. User accepts visuals/audio; do not generate
large evidence sets. Commit meaningful grouped milestones with TASK IDs and
short actual validation notes. Do not mark unmet native/provider checks PASS.

TASK-0202/0203 run early after platform composition, proving the shared app,
Skia Battle, touch and restart. TASK-0234/0237 rebuild and smoke the completed
candidate. Require playable required screens, complete core/meta/ads/platform
behavior, durable progress, no known blocking runtime defects, runnable Android
APK/AAB and iOS Simulator app. Distribution signing/store submission/GitLab
publication ceremony can wait. Owner-only provider/legal/signing inputs must be
recorded precisely after independent work; they cannot be replaced by fake
production success. DELIVERY_COMPLETE remains an actual product acceptance,
not the outcome of a planning validator or a time limit.

Owner-only live Start.io inventory verification (TASK-0259) gates TASK-0237 directly. It does not block independent TASK-0229 feature closure/optimization and candidate builds; the requirement remains required and unverified until owner app IDs are available.

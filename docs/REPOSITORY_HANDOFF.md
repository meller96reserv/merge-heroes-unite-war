# COCOS-HANDOFF — очистка и передача репозитория

Дата: 2026-09-11. Пользователь разрешил удалить лишнее и объединить актуальную Cocos-разработку в main. Предыдущее полное дерево: `3b81e20`.

- Удалены RN/Expo приложение, дублирующий core, host/dependencies, старый мост/spike, его генераторы и повторяющиеся RN QA-отчёты. 245 прежних документов/схем перенесены в явно исторический `archive/rn-planning/`.
- Обновлены README, AGENTS, execution rules, start prompt, индекс документации; добавлены карта текущего кода/ресурсов, инструкции для чистого клона и список реальных ограничений.
- Cocos проверяется с собственной единственной зависимостью TypeScript и lockfile. Убраны fallback-пути к удалённому RN art. Упаковка исходников не зависит от локальных audio-sources или benchmark.
- Android release без существующего приватного ключа останавливается; ключ не регенерируется автоматически. Историческая release evidence 1.8-9 сохранена в Git; новая упаковка по умолчанию получает NOT_RUN.
- Игровые assets/scripts, native-код и branding не менялись. Готовые 1.8-9, signing/Telegraph credentials, Figma originals, исходные референсные записи и данные эмуляторов сохранены локально.

Проверки после правок:

| Проверка | Результат |
| --- | --- |
| Изолированное дерево из Git index, без локальных зависимостей/ключей | npm ci и focused core integration PASS |
| TypeScript с текущими Creator typings | PASS |
| Python syntax и ссылки текущих входных документов | PASS |
| Source-only ZIP из изолированного дерева | PASS: 439 файлов, без private/node_modules/audio-sources/benchmark/build |
| Отсутствующий release key | PASS: понятный отказ, новый ключ не создан |
| Staged security scan и whitespace | PASS, без отключения hook |
| Повторная нативная сборка/геймплей | NOT_RUN: runtime не менялся, задача ограничена репозиторием/сборочными помощниками |

Архивные ссылки могут отражать прежние пути; для полного исторического контекста использовать commit выше. История Git не переписана. Для новой разработки достаточно shallow clone main и актуальных входных документов.
